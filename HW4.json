{
  "info": {
    "_postman_id": "02375ee3-8586-4c4e-a5af-7a55ba09bd60",
    "name": "HW4 - Registration Validation",
    "description": "Data Driven Postman collection for registration validation on qauto and qauto2.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "register validation",
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "exec": [
              "function getParam(key, fallback = \"\") {",
              "    var val = pm.iterationData.get(key);",
              "    return (val !== undefined && val !== null) ? val : fallback;",
              "}",
              "",
              "pm.variables.set(\"testName\", getParam(\"name\"));",
              "pm.variables.set(\"testLastName\", getParam(\"lastName\"));",
              "",
              "var rawEmail = getParam(\"email\");",
              "var emailToSend = (typeof rawEmail === 'string' && rawEmail.trim() !== \"\") ",
              "    ? rawEmail ",
              "    : `auto_user_${pm.info.iteration}_${Date.now()}@test.com`;",
              "",
              "pm.variables.set(\"testEmail\", emailToSend);",
              "pm.variables.set(\"testPassword\", getParam(\"password\"));",
              "pm.variables.set(\"testRepeatPassword\", getParam(\"repeatPassword\"));",
              "",
              "var expectedStatus = Number(getParam(\"expectedStatus\", 201));",
              "pm.variables.set(\"expectedStatus\", Number.isFinite(expectedStatus) ? expectedStatus : 201);"
            ],
            "type": "text/javascript"
          }
        },
        {
          "listen": "test",
          "script": {
            "exec": [
              "let jsonData = {};",
              "try {",
              "    jsonData = pm.response.json();",
              "} catch (e) {",
              "    console.log(\"Response is not valid JSON or is empty\");",
              "}",
              "",
              "const expectedStatus = Number(pm.variables.get(\"expectedStatus\"));",
              "const testCase = pm.iterationData.get(\"case\") || `iteration ${pm.info.iteration + 1}`;",
              "",
              "pm.test(`[${testCase}] expected status`, function () {",
              "    pm.expect(pm.response.code).to.eql(expectedStatus);",
              "});",
              "",
              "if (expectedStatus >= 200 && expectedStatus < 300) {",
              "    pm.test(`[${testCase}] successful registration`, function () {",
              "        pm.expect(jsonData.status).to.eql(\"ok\");",
              "        pm.expect(jsonData.data).to.be.an(\"object\");",
              "    });",
              "} else {",
              "    pm.test(`[${testCase}] validation error response`, function () {",
              "        pm.expect(pm.response.code).to.be.within(400, 499);",
              "    });",
              "}"
            ],
            "type": "text/javascript"
          }
        }
      ],
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"name\": \"{{testName}}\",\n  \"lastName\": \"{{testLastName}}\",\n  \"email\": \"{{testEmail}}\",\n  \"password\": \"{{testPassword}}\",\n  \"repeatPassword\": \"{{testRepeatPassword}}\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        },
        "url": {
          "raw": "{{baseUrl}}/auth/signup",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "auth",
            "signup"
          ]
        }
      },
      "response": []
    }
  ],
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "var iterationBaseUrl = pm.iterationData.get(\"baseUrl\");",
          "if (iterationBaseUrl) {",
          "    pm.variables.set(\"baseUrl\", iterationBaseUrl);",
          "}"
        ]
      }
    }
  ],
  "variable": [
    {
      "key": "baseUrl",
      "value": ""
    }
  ]
}
